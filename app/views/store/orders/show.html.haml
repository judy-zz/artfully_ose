%ul.ticket-list
  - current_order.items.each do |item|
    %li= render item
- unless @donations.empty?
  - @donations.each do |donation|
    = render :partial => 'store/donations/form', :locals => { :target => donation }
= link_to "Clear Cart", store_order_path(current_order), :method => :delete, :confirm => 'Are you sure?' unless current_order.empty?