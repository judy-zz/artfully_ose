%span.price= number_as_cents purchasable_ticket.price
%span.item= "#{purchasable_ticket.ticket.event} at #{l purchasable_ticket.ticket.show.in_time_zone(Event.find(purchasable_ticket.ticket.event_id).time_zone), :format => :short}"
