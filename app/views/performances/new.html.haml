= render "shared/error_messages", :target => @performance
= form_for @performance, :url => event_performances_path(@event, @performance) do |form|
  #performance
    %h3 New Performance
    %ul
      = render :partial => 'performances/form', :locals => { :parent_form => form, :target => @performance }
      %li= form.submit "Submit"