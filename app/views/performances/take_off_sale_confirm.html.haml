- content_for :header do
  %h1 Take off Sale

%p= "You are taking all tickets off sale for the #{@performance.formatted_performance_date}, #{@performance.formatted_performance_time(@performance.event.time_zone)} performance of #{@performance.event.name}"

= form_for @performance, :url => take_performance_off_sale_path(@performance) do |form|
  = hidden_field_tag :confirm, :true
  %p If this is correct, please click confirm it by clicking
  = submit_tag "Take off Sale"

%p
  If this is not correct,
  = link_to "cancel this action and return to the performance page", performance_url(@performance)