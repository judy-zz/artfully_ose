= render "shared/error_messages", :target => @section
= form_for @section, :url => event_chart_sections_path(@event, @chart, @section) do |form|
  #section
    %h3 New Section
    = render :partial => 'sections/form', :locals => { :parent_form => form, :target => @section }
    = form.submit "Submit"