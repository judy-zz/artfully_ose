- content_for :header do
  %h1= t(@kit.type.underscore)

.spread
  %p
    You meet the requirements for this kit.
    = link_to "Activate #{t(@kit.type.underscore)}", kits_path(:type => @kit.type.underscore), :method => :post
