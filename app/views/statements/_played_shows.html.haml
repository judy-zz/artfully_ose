- if played.empty?
  %p There are no played shows.
- else
  %ul.detailed-list
    - played.each do |performance|
      %li
        %h4= link_to (l performance.datetime, :format => :short), statement_path(performance), :title => 'performance-datetime'
