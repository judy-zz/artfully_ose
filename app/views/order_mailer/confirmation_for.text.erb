Thanks for your order! Here's what you purchased:

<% unless @order.tickets.empty? %>
  Tickets
  <% @order.tickets.each do |ticket| %>
    - <%= "#{ticket.event} at #{l ticket.performance, :format => :short}, $#{ticket.price/100.00}" %>
  <% end %>
<% end %>

<% unless @order.donations.empty? %>
  Donations
  <% @order.donations.each do |donation| %>
    - <%= "$#{donation.amount/100.00} donation to #{donation.organization.name}" %>
  <% end %>
<% end %>