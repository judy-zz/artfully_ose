<p>Hello <%= @resource.email %>!</p>

<% if @resource.invited_by.present? %>
  <p><%= @resource.invited_by.email %> has invited you to join <%= link_to("Artful.ly", root_url) %>. You can accept it through the link below.
<% else %>
  <p>You have been invited to join <%= link_to("Artful.ly", root_url) %>. You can accept it through the link below.
<% end %>

<p><%= link_to 'Accept invitation', accept_invitation_url(@resource, :invitation_token => @resource.invitation_token) %></p>

<p>If you don't want to accept the invitation, please ignore this email.<br />
Your account won't be created until you access the link above and set your password.</p>