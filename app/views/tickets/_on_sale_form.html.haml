#on-sale.grouped-form
  %h3 Put on Sale
  = form_tag on_sale_performance_tickets_path(performance), :method => :put do |form|
    = hidden_field_tag :performance_id, performance.id
    %ul
      - tickets.each do |ticket|
        %li
          .label-and-element
            = check_box_tag "selected_tickets[]", ticket.id, false, :id => "on_sale_ticket_#{ticket.id}", :class => "item_#{ticket.id}"
            = label_tag "item_#{ticket.id}", ticket.id
    = submit_tag "Put on Sale"