.context
.document
  - if @kit.requirements_met?
    %p
      You meet the requirements for this kit.
      = link_to "Activate #{@kit.type.underscore.titleize}", kits_path(:type => @kit.type.underscore), :method => :post
  - else
    %p You do not meet the requirements for this kit.